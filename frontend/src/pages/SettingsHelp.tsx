import { useMutation, useQuery } from "@tanstack/react-query";
import { useEffect, useRef, useState } from "react";
import { BookOpen, Download, FileJson2, Github, KeyRound, LifeBuoy, LockKeyhole, Moon, RotateCcw, Save, ShieldCheck, Sun, Upload } from "lucide-react";
import { Link } from "react-router-dom";
import { api } from "../lib/api";
import { useProduct } from "../state/ProductContext";
import { Badge, Button, Callout, DataList, Field, PageHeader, Panel, PanelHeader } from "../components/Primitives";

type Theme = "dark" | "light" | "system";

export function SettingsPage() {
  const { runConfig, setRunConfig } = useProduct(); const [theme, setTheme] = useState<Theme>(() => (window.localStorage.getItem("bluefire.theme") as Theme) || "dark"); const [retention, setRetention] = useState(30); const [exportPath, setExportPath] = useState("bluefire-exports"); const fileRef = useRef<HTMLInputElement>(null); const [notice, setNotice] = useState<string>();
  const hydrated = useRef(false); const settingsQuery = useQuery({ queryKey: ["settings"], queryFn: api.settings });
  useEffect(() => { if (hydrated.current || !settingsQuery.data) return; hydrated.current = true; const value = settingsQuery.data.settings.find((item) => item.key === "ui.preferences")?.value as Record<string, unknown> | undefined; if (!value) return; if (value.theme === "dark" || value.theme === "light" || value.theme === "system") setTheme(value.theme); if (typeof value.retention_days === "number") setRetention(value.retention_days); if (typeof value.export_path === "string") setExportPath(value.export_path); if (value.run_defaults && typeof value.run_defaults === "object") setRunConfig({ ...runConfig, ...(value.run_defaults as Partial<typeof runConfig>), approved: false, approvedBy: "" }); }, [runConfig, setRunConfig, settingsQuery.data]);
  useEffect(() => { window.localStorage.setItem("bluefire.theme", theme); const resolved = theme === "system" ? (window.matchMedia("(prefers-color-scheme: light)").matches ? "light" : "dark") : theme; document.documentElement.dataset.theme = resolved; }, [theme]);
  const preferenceDocument = () => { const { approved, approvedBy, endpoint, ...runDefaults } = runConfig; void approved; void approvedBy; void endpoint; return { schema_version: "bluefire.ui-settings.v1", theme, run_defaults: runDefaults, retention_days: retention, export_path: exportPath }; };
  const saveMutation = useMutation({ mutationFn: () => api.saveSetting("ui.preferences", preferenceDocument()), onSuccess: ({ setting }) => setNotice(`Settings saved durably at ${new Date(setting.updated_at).toLocaleString()}. Execute approval remained cleared.`), onError: (error) => setNotice(error instanceof Error ? error.message : "Settings could not be saved.") });
  const exportSettings = () => { const payload = { ...preferenceDocument(), secret_values: "never_exported" }; const url = URL.createObjectURL(new Blob([`${JSON.stringify(payload, null, 2)}\n`], { type: "application/json" })); const link = document.createElement("a"); link.href = url; link.download = "bluefire-settings.json"; link.click(); URL.revokeObjectURL(url); };
  const importSettings = async (file?: File) => { if (!file) return; try { const payload = JSON.parse(await file.text()); if (payload.schema_version !== "bluefire.ui-settings.v1") throw new Error("Unsupported settings document."); if (payload.theme) setTheme(payload.theme); if (payload.run_defaults) setRunConfig({ ...runConfig, ...payload.run_defaults, approved: false, approvedBy: "", endpoint: "" }); if (Number.isFinite(payload.retention_days)) setRetention(payload.retention_days); if (typeof payload.export_path === "string") setExportPath(payload.export_path); setNotice("Settings imported into this form. Review and save them; Execute approval was intentionally cleared."); } catch (error) { setNotice(error instanceof Error ? error.message : "Import failed."); } finally { if (fileRef.current) fileRef.current.value = ""; } };
  return <div className="page settings-page"><PageHeader eyebrow="Application configuration" title="Settings" description="Manage durable local defaults, retention preferences, exports, themes, and environment references without persisting plaintext secrets." actions={<><input className="sr-only" ref={fileRef} type="file" accept="application/json" onChange={(event) => importSettings(event.target.files?.[0])}/><Button variant="secondary" onClick={() => fileRef.current?.click()}><Upload/>Import</Button><Button variant="secondary" onClick={exportSettings}><Download/>Export</Button><Button variant="primary" onClick={() => saveMutation.mutate()} disabled={saveMutation.isPending}><Save/>{saveMutation.isPending ? "Saving" : "Save settings"}</Button></>} />
    {notice ? <Callout title="Settings">{notice}</Callout> : settingsQuery.isError ? <Callout tone="warning" title="Durable settings unavailable">The form is using browser defaults. Save after the local service is ready.</Callout> : null}<div className="settings-grid"><Panel><PanelHeader eyebrow="Appearance" title="Workspace theme"/><div className="detail-body"><div className="theme-picker">{(["dark", "light", "system"] as Theme[]).map((item) => <button key={item} className={theme === item ? "selected" : ""} onClick={() => setTheme(item)}>{item === "dark" ? <Moon/> : item === "light" ? <Sun/> : <RotateCcw/>}<span><strong>{item[0]!.toUpperCase() + item.slice(1)}</strong><small>{item === "system" ? "Follow operating system" : `${item} interface`}</small></span></button>)}</div></div></Panel><Panel><PanelHeader eyebrow="Safety defaults" title="New run defaults"/><div className="detail-body"><Field label="Effect mode"><select value={runConfig.mode} onChange={(event) => setRunConfig({ ...runConfig, mode: event.target.value as "simulate" | "execute", approved: false, approvedBy: "" })}><option value="simulate">Simulate</option><option value="execute">Execute</option></select></Field><Field label="AI autonomy"><select value={runConfig.autonomy} onChange={(event) => setRunConfig({ ...runConfig, autonomy: event.target.value as typeof runConfig.autonomy })}><option value="off">Off</option><option value="assist">Assist</option><option value="auto">Auto</option></select></Field><Field label="Maximum safety tier"><select value={runConfig.safetyTier} onChange={(event) => setRunConfig({ ...runConfig, safetyTier: event.target.value as typeof runConfig.safetyTier })}><option value="safe">Safe</option><option value="controlled">Controlled</option><option value="restricted">Restricted</option></select></Field><Callout title="Approval never persists">Execute confirmation and operator identity are cleared on load and import and are never part of saved defaults.</Callout></div></Panel><Panel><PanelHeader eyebrow="Storage" title="Run retention & exports"/><div className="detail-body"><Field label="Retention days" hint="Saved as an operator preference; automatic run deletion is not implemented."><input type="number" min={1} max={3650} value={retention} onChange={(event) => setRetention(Number(event.target.value))}/></Field><Field label="Export path reference" hint="A relative local reference; the browser cannot write arbitrary paths."><input value={exportPath} onChange={(event) => setExportPath(event.target.value)} pattern="[A-Za-z0-9._/-]+"/></Field><DataList items={[{ label: "Run storage", value: "Canonical local run store" }, { label: "Evidence", value: "Immutable bundle records where practical" }, { label: "Secrets", value: "Environment references only" }]} /></div></Panel><Panel><PanelHeader eyebrow="Integrations" title="Secret references"/><div className="detail-body"><div className="secret-row"><span><KeyRound/></span><div><strong>OPENAI_API_KEY</strong><small>Environment reference · value never read by browser</small></div><Badge tone="neutral">Not checked</Badge></div><div className="secret-row"><span><LockKeyhole/></span><div><strong>BLUEFIRE_RUNNER_BINARY</strong><small>Runner boundary reference · resolved server-side</small></div><Badge tone="neutral">Not checked</Badge></div><Callout title="No plaintext secret fields">Provider tokens, runner credentials, and backend credentials must be resolved at the boundary that needs them.</Callout></div></Panel></div>
  </div>;
}

const helpCards = [
  { title: "First Simulate run", icon: ShieldCheck, to: "/runs", text: "Open a scenario, validate the graph, choose Simulate with AI Off, review preflight, then inspect synthetic evidence." },
  { title: "Prepare Execute", icon: LockKeyhole, to: "/runners", text: "Register a runner record, verify the executor outside the browser, select an Execute profile and scope, then review the exact approval envelope." },
  { title: "Tune a detection", icon: FileJson2, to: "/detection-lab", text: "Link a behavior hypothesis, parse with an authoritative backend when available, exercise fixtures, attach observed evidence, and evaluate benign records." },
  { title: "Replay a defense change", icon: RotateCcw, to: "/compare", text: "Select an immutable source run, declare the defense change, choose an available safe replay strategy, then compare canonical deltas." },
];

export function HelpPage() {
  return <div className="page help-page"><PageHeader eyebrow="Documentation & support" title="Help center" description="Operate BlueFire safely, understand evidence semantics, and troubleshoot missing runners, collectors, providers, and detection backends." actions={<a href="https://github.com/Moneer-S/BlueFire-Nexus" target="_blank" rel="noreferrer"><Button variant="secondary"><Github/>Repository</Button></a>} />
    <div className="help-grid">{helpCards.map((card) => { const Icon = card.icon; return <Panel key={card.title}><Icon/><h2>{card.title}</h2><p>{card.text}</p><Link to={card.to}><Button size="small" variant="ghost">Open workflow</Button></Link></Panel>; })}</div>
    <div className="two-column"><Panel><PanelHeader eyebrow="Core concepts" title="Know what each record proves"/><div className="concept-list"><div><Badge tone="info">Synthetic</Badge><p>Modeled or fixture-generated evidence. It is not proof of execution.</p></div><div><Badge tone="warning">Executed</Badge><p>A runner reports that an action started or completed. It is not independent observation.</p></div><div><Badge tone="success">Observed</Badge><p>An independent declared collector attributed an observable to the experiment.</p></div><div><Badge tone="warning">Control blocked</Badge><p>A control or policy prevented the requested path; this is a valuable outcome.</p></div><div><Badge tone="violet">Counterfactual</Badge><p>A modeled continuation after a real path stopped. It must remain visibly distinct.</p></div></div></Panel><Panel><PanelHeader eyebrow="Troubleshooting" title="Common readiness problems"/><details className="help-details"><summary>Execute preflight says runner unavailable</summary><p>Confirm the Rust runner binary reference, sandbox root, identity, version compatibility, profile attachment, and advertised actions. Do not change to a broader scope to make preflight pass.</p></details><details className="help-details"><summary>AI provider is not healthy</summary><p>Use AI Off for deterministic operation. Verify only the environment-variable reference, endpoint, model, timeout, and provider health; never paste a secret into the UI.</p></details><details className="help-details"><summary>Observed evidence is missing</summary><p>Runner receipts are executed evidence. Enable and verify an independent collector, inspect its health and timeout, then replay if the environment is still authorized.</p></details><details className="help-details"><summary>A rendered detection did not match</summary><p>Parsing, fixture exercise, observed exercise, and benign evaluation are separate lifecycle stages. Inspect field mapping and telemetry prerequisites before tuning.</p></details></Panel></div>
    <Panel><PanelHeader eyebrow="Quick navigation" title="Continue in the product"/><div className="quick-links"><Link to="/scenarios"><BookOpen/>Choose a scenario</Link><Link to="/builder"><FileJson2/>Build the graph</Link><Link to="/runs"><ShieldCheck/>Configure a run</Link><Link to="/detection-lab"><LifeBuoy/>Open Detection Lab</Link></div></Panel>
  </div>;
}
